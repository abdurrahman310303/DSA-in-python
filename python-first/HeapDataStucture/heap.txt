
Heap is a complete binary tree which will follow the following Heap Properties

1. if it is a min heap it should follow the property like the root must be the smallest in the 
    whole tree and the values will increase as we go towards the leafs

if we represent a binary tree through an array so

    the left child will be at index 2*i + 1
    the right child will be at index 2*i + 2

    